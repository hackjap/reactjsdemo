apiVersion: v1
kind: Service
metadata:
  name: front-service
  namespace: default
spec:
  selector:
    tier: front-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      NodePort: 30100
  type: nodePort